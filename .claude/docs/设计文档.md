# 后端交互设计文档

## 一、文档说明

本文档定义了 NestJS 后端开发的完整交互设计规范，采用**六层结构**组织代码，确保代码清晰、可维护、可复用。

### 六层结构说明

```
┌─────────────────────────────────────────┐
│  1. 实体层 (Entity)                    │  ← 数据库映射，最先定义
├─────────────────────────────────────────┤
│  2. 常量层 (Constants)                  │  ← 业务常量、枚举、配置
├─────────────────────────────────────────┤
│  3. 类型层 (Types)                      │  ← TypeScript 类型定义
├─────────────────────────────────────────┤
│  4. DTO 层 (Data Transfer Object)       │  ← 数据传输对象、验证规则
├─────────────────────────────────────────┤
│  5. 服务层 (Service)                    │  ← 业务逻辑、数据处理
├─────────────────────────────────────────┤
│  6. 控制器层 (Controller)                │  ← HTTP 请求处理、路由定义
└─────────────────────────────────────────┘
```

### 设计原则

1. **职责清晰**：每一层专注于特定功能，避免混杂
2. **自下而上**：从实体到控制器，逐层构建
3. **依赖单向**：下层不依赖上层，便于测试和维护
4. **易于阅读**：代码结构一目了然，快速定位功能
5. **类型优先**：先定义 Entity 和 Types，再定义 DTO 和 Service

---

## 二、技术规范基石

本文档基于以下三个核心规范文档：

1. **[项目 AI 代码生成规范](../.github/copilot-instructions.md)**
   - 技术栈规范（NestJS + TypeORM + TypeScript）
   - 代码编写规范（注释、命名、错误处理）
   - 模块开发规范（Controller、Service、Entity）

2. **[NestJS 官方文档](https://docs.nestjs.com)**
   - 模块系统
   - 依赖注入
   - 控制器和服务

3. **[TypeORM 文档](https://typeorm.io)**
   - 实体定义
   - 仓储模式
   - 查询构建

**请在开发前务必阅读以上规范文档！**

---

## 三、技术栈与核心组件

### 3.1 技术栈

```typescript
// 核心技术栈
NestJS 11.x + TypeScript 5.x
TypeORM 0.3.x + MySQL 8.x
class-validator + class-transformer (DTO 验证)
JWT (@nestjs/jwt) 认证
腾讯云 SMS SDK
```

### 3.2 项目通用组件

#### 响应格式化拦截器
```typescript
import { ResponseHelper } from '@/common/utils/response.helper'

// 成功响应
return ResponseHelper.success(data, '操作成功')

// 分页响应
return ResponseHelper.paginated(
  list,      // 数据列表
  total,     // 总数
  page,      // 当前页码
  limit,     // 每页数量
  '查询成功'
)

// 错误响应
return ResponseHelper.error('错误信息', { code: 'ERROR_CODE' })
```

#### 统一响应拦截器
项目已配置全局响应拦截器，所有接口自动包装为标准格式：
```typescript
// 接口返回格式
{
  success: true,
  message: '操作成功',
  data: {
    // 实际数据
  },
  timestamp: '2024-01-01T00:00:00.000Z',
  path: '/api/users'
}
```

#### 错误过滤器
```typescript
import { HttpExceptionFilter } from '@/common/filters/http-exception.filter'

// 使用方式（在模块中全局注册或控制器级别使用）
@UseFilters(HttpExceptionFilter)
@Controller('api/users')
export class UserController {}
```

### 3.3 常用装饰器

```typescript
// 控制器装饰器
@Controller('api/users')           // 路由前缀
@Get()                              // GET 请求
@Post()                             // POST 请求
@Put(':id')                         // PUT 请求（带参数）
@Delete(':id')                      // DELETE 请求
@HttpCode(204)                      // 自定义 HTTP 状态码

// 参数装饰器
@Body()                             // 请求体
@Param('id', ParseUUIDPipe)         // URL 参数（带类型转换）
@Query('page', DefaultValuePipe(1))  // 查询参数（带默认值）
@Headers('authorization')            // 请求头

// 认证授权装饰器
@UseGuards(JwtAuthGuard)            // JWT 认证守卫
@UseGuards(AdminOnlyGuard)           // 管理员权限守卫
```

---

## 四、示例一：用户管理模块

### 功能描述
基础的 CRUD 模块，包含：用户列表查询、用户详情、用户更新、用户删除。

### 完整代码示例

### 4.1 模块结构

```
user/
├── constants/              # 常量层
│   ├── index.ts
│   └── user.constants.ts
├── dto/                   # DTO 层
│   ├── index.ts
│   └── update-user.dto.ts
├── entities/              # 实体层
│   └── user.entity.ts
├── services/              # 服务层
│   ├── user.service.ts
│   └── encryption.service.ts
├── types/                 # 类型定义
│   └── user.types.ts
├── user.controller.ts     # 控制器层
└── user.module.ts        # 模块定义
```

### 4.2 常量层

```typescript
// src/modules/user/constants/user.constants.ts

/**
 * 用户模块常量定义
 */
export const USER_CONSTANTS = {
  // 分页配置
  PAGINATION: {
    DEFAULT_PAGE: 1,
    DEFAULT_LIMIT: 10,
    MAX_LIMIT: 100
  },
  // 字段选择配置
  SELECT_FIELDS: {
    // 公开字段（不含敏感信息）
    PUBLIC: ['id', 'username', 'nickname', 'phone', 'email', 'avatar', 'status', 'createdAt', 'updatedAt'] as const,
    // 内部字段（用于认证和内部逻辑）
    INTERNAL: ['id', 'username', 'nickname', 'phone', 'phoneHash', 'userId', 'email', 'password', 'status', 'createdAt', 'updatedAt'] as const
  },
  // 状态枚举
  STATUS: {
    ACTIVE: 1,
    INACTIVE: 0
  }
} as const

export default USER_CONSTANTS
```

```typescript
// src/modules/user/constants/index.ts
export * from './user.constants'
```

### 4.3 DTO 层

```typescript
// src/modules/user/dto/update-user.dto.ts

import { IsString, IsOptional, IsEnum, IsEmail, MinLength, MaxLength } from 'class-validator'
import { USER_CONSTANTS } from '@/modules/user/constants'

/**
 * 更新用户 DTO
 */
export class UpdateUserDto {
  @IsString()
  @IsOptional()
  @MinLength(2)
  @MaxLength(50)
  nickname?: string

  @IsString()
  @IsOptional()
  @IsEmail()
  email?: string

  @IsString()
  @IsOptional()
  phone?: string

  @IsString()
  @IsOptional()
  avatar?: string

  @IsEnum(USER_CONSTANTS.STATUS)
  @IsOptional()
  status?: number
}
```

```typescript
// src/modules/user/dto/index.ts
export * from './update-user.dto'
```

### 4.4 实体层

```typescript
// src/modules/user/entities/user.entity.ts

import { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, UpdateDateColumn } from 'typeorm'

/**
 * 用户实体
 */
@Entity('users')
export class User {
  @PrimaryGeneratedColumn('uuid')
  id: string

  @Column({ length: 50, unique: true })
  username: string

  @Column({ length: 50, nullable: true })
  nickname: string

  @Column({ length: 255, nullable: true })
  phone: string

  @Column({ length: 64, nullable: true })
  phoneHash: string

  @Column({ length: 64, nullable: true })
  userId: string

  @Column({ length: 255, nullable: true })
  email: string

  @Column({ length: 255, nullable: true })
  password: string

  @Column({ length: 500, nullable: true })
  avatar: string

  @Column({ type: 'tinyint', default: 1 })
  status: number

  @Column({ type: 'tinyint', default: 1 })
  isActive: number

  @Column({ type: 'varchar', length: 6, nullable: true })
  verificationCode: string

  @Column({ type: 'datetime', nullable: true })
  verificationCodeExpiredAt: Date

  @Column({ type: 'datetime', nullable: true })
  lastCodeSentAt: Date

  @CreateDateColumn()
  createdAt: Date

  @UpdateDateColumn()
  updatedAt: Date
}
```

### 4.5 服务层

```typescript
// src/modules/user/services/user.service.ts

import { Injectable, NotFoundException, InternalServerErrorException } from '@nestjs/common'
import { InjectRepository } from '@nestjs/typeorm'
import { Repository } from 'typeorm'
import { User } from '@/modules/user/entities/user.entity'
import { UpdateUserDto } from '@/modules/user/dto'
import { EncryptionService } from '@/modules/user/services/encryption.service'
import { ApiResponse } from '@/common/interceptors/response.interceptor'
import { ResponseHelper } from '@/common/utils/response.helper'
import { USER_CONSTANTS } from '@/modules/user/constants'
import { PublicUser, InternalUser, UserPaginatedResult, UserDeleteResult } from '@/modules/user/types/user.types'

/**
 * 用户服务
 * 负责用户数据的增删改查操作
 */
@Injectable()
export class UserService {
  constructor(
    @InjectRepository(User)
    private readonly userRepository: Repository<User>,
    private readonly encryptionService: EncryptionService,
  ) {}

  /**
   * 根据ID查找用户（不包含敏感字段，返回解密后的手机号）
   */
  async getUserById(id: string): Promise<ApiResponse<PublicUser | null>> {
    try {
      const user = await this.userRepository.findOne({
        where: { id, isActive: true },
        select: USER_CONSTANTS.SELECT_FIELDS.PUBLIC
      })
      
      if (!user) {
        throw new NotFoundException('用户不存在')
      }

      // 解密手机号并返回（排除敏感字段）
      const userData = this._getUserWithDecryptedPhone(user)
      
      return ResponseHelper.success(userData, '用户查询成功')
    } catch (error) {
      if (error instanceof NotFoundException) {
        throw error
      }
      throw new InternalServerErrorException(
        '用户查询失败，请稍后重试',
        { cause: error }
      )
    }
  }

  /**
   * 辅助方法：返回包含解密手机号的用户信息（排除敏感字段）
   */
  private _getUserWithDecryptedPhone(user: User): PublicUser {
    try {
      const decryptedPhone = this.encryptionService.decryptPhone(user.phone)
      const { userId, phoneHash, verificationCode, verificationCodeExpiredAt, lastCodeSentAt, ...result } = user
      return {
        ...result,
        phone: decryptedPhone
      }
    } catch (error) {
      throw new InternalServerErrorException(
        '用户数据解密失败',
        { cause: error }
      )
    }
  }

  /**
   * 内部方法：获取包含 userId 的完整用户信息（用于系统内部调用）
   * 仅用于认证和内部业务逻辑，不对外暴露
   */
  async getUserByIdInternal(id: string): Promise<InternalUser | null> {
    try {
      return await this.userRepository.findOne({
        where: { id, isActive: true },
        select: USER_CONSTANTS.SELECT_FIELDS.INTERNAL
      })
    } catch (error) {
      throw new InternalServerErrorException(
        '用户查询失败，请稍后重试',
        { cause: error }
      )
    }
  }

  /**
   * 更新用户信息
   */
  async updateUser(id: string, updateUserDto: UpdateUserDto): Promise<ApiResponse<PublicUser | null>> {
    try {
      const user = await this.userRepository.findOne({ 
        where: { id, isActive: true },
        select: ['id']
      })
      
      if (!user) {
        throw new NotFoundException('用户不存在')
      }

      // 如果要更新手机号，需要加密处理
      const updateData = { ...updateUserDto }
      if (updateUserDto.phone) {
        updateData.phone = this.encryptionService.encryptPhone(updateUserDto.phone)
        updateData['phoneHash'] = this.encryptionService.hashPhone(updateUserDto.phone)
      }

      await this.userRepository.update(id, updateData)

      const userResult = await this.getUserById(id)
      return ResponseHelper.success(userResult.data, '用户更新成功')
    } catch (error) {
      if (error instanceof NotFoundException) {
        throw error
      }
      throw new InternalServerErrorException(
        '用户更新失败，请稍后重试',
        { cause: error }
      )
    }
  }

  /**
   * 软删除用户（设置 isActive 为 false）
   */
  async deleteUser(id: string): Promise<ApiResponse<UserDeleteResult>> {
    try {
      const user = await this.userRepository.findOne({ 
        where: { id, isActive: true },
        select: ['id']
      })
      
      if (!user) {
        throw new NotFoundException('用户不存在')
      }

      const result = await this.userRepository.update(id, { isActive: false })
      const success = (result.affected ?? 0) > 0

      if (success) {
        return ResponseHelper.success(
          { deleted: true }, 
          '用户删除成功'
        )
      } else {
        return ResponseHelper.error(
          '用户删除失败，请稍后重试', 
          { deleted: false }
        )
      }
    } catch (error) {
      if (error instanceof NotFoundException) {
        throw error
      }
      throw new InternalServerErrorException(
        '用户删除失败，请稍后重试',
        { cause: error }
      )
    }
  }

  /**
   * 获取所有用户（分页）
   */
  async getUserList(
    page: number = USER_CONSTANTS.PAGINATION.DEFAULT_PAGE, 
    limit: number = USER_CONSTANTS.PAGINATION.DEFAULT_LIMIT
  ): Promise<ApiResponse<UserPaginatedResult>> {
    try {
      const safeLimit = Math.min(limit, USER_CONSTANTS.PAGINATION.MAX_LIMIT)
      
      const [users, total] = await this.userRepository.findAndCount({
        where: { isActive: true },
        select: USER_CONSTANTS.SELECT_FIELDS.PUBLIC,
        skip: (page - 1) * safeLimit,
        take: safeLimit,
        order: { createdAt: 'DESC' }
      })

      // 解密所有用户的手机号并排除敏感字段
      const decryptedUsers = users.map(user => this._getUserWithDecryptedPhone(user))

      return ResponseHelper.paginated(
        decryptedUsers, 
        total, 
        page, 
        safeLimit, 
        '用户列表查询成功'
      )
    } catch (error) {
      throw new InternalServerErrorException(
        '用户查询失败，请稍后重试',
        { cause: error }
      )
    }
  }
}
```

### 4.6 控制器层

```typescript
// src/modules/user/user.controller.ts

import { 
  Controller, 
  Get, 
  Put, 
  Delete, 
  Body, 
  Param, 
  Query, 
  ParseIntPipe, 
  ParseUUIDPipe,
  HttpCode,
  DefaultValuePipe,
} from '@nestjs/common'
import { UserService } from '@/modules/user/services/user.service'
import { UpdateUserDto } from '@/modules/user/dto'
import { USER_CONSTANTS } from '@/modules/user/constants'

/**
 * 用户控制器
 * 处理用户管理相关的HTTP请求
 */
@Controller('api/users')
export class UserController {
  constructor(
    private readonly userService: UserService,
  ) {}

  /**
   * 获取所有用户（分页）
   * 
   * @param page 页码，默认为1
   * @param limit 每页数量，默认为10，最大100
   */
  @Get()
  async getUserList(
    @Query('page', new DefaultValuePipe(USER_CONSTANTS.PAGINATION.DEFAULT_PAGE), ParseIntPipe) 
    page: number,
    @Query('limit', new DefaultValuePipe(USER_CONSTANTS.PAGINATION.DEFAULT_LIMIT), ParseIntPipe) 
    limit: number,
  ) {
    return await this.userService.getUserList(page, limit)
  }

  /**
   * 根据ID获取用户信息
   * 
   * @param id 用户的UUID标识符
   */
  @Get(':id')
  async getUserById(@Param('id', ParseUUIDPipe) id: string) {
    return await this.userService.getUserById(id)
  }

  /**
   * 更新用户信息
   * 
   * @param id 用户的UUID标识符
   * @param updateUserDto 更新用户信息的数据传输对象
   */
  @Put(':id')
  async updateUser(
    @Param('id', ParseUUIDPipe) id: string,
    @Body() updateUserDto: UpdateUserDto,
  ) {
    return await this.userService.updateUser(id, updateUserDto)
  }

  /**
   * 删除用户（软删除）
   * 
   * @param id 用户的UUID标识符
   */
  @Delete(':id')
  @HttpCode(204)
  async deleteUser(@Param('id', ParseUUIDPipe) id: string) {
    return await this.userService.deleteUser(id)
  }
}
```

### 4.7 模块定义

```typescript
// src/modules/user/user.module.ts

import { Module } from '@nestjs/common'
import { TypeOrmModule } from '@nestjs/typeorm'
import { UserController } from '@/modules/user/user.controller'
import { UserService } from '@/modules/user/services/user.service'
import { User } from '@/modules/user/entities/user.entity'
import { EncryptionService } from '@/modules/user/services/encryption.service'

/**
 * 用户模块
 * 包含用户相关的所有功能：控制器、服务、实体等
 */
@Module({
  imports: [
    // 注册用户实体
    TypeOrmModule.forFeature([User])
  ],
  controllers: [UserController],
  providers: [UserService, EncryptionService],
  exports: [UserService, EncryptionService],
})
export class UserModule {}
```

### 4.8 类型定义

```typescript
// src/modules/user/types/user.types.ts

/**
 * 公开用户信息（不含敏感字段）
 */
export interface PublicUser {
  id: string
  username: string
  nickname?: string
  phone: string
  email?: string
  avatar?: string
  status: number
  createdAt: Date
  updatedAt: Date
}

/**
 * 内部用户信息（包含敏感字段，仅用于内部逻辑）
 */
export interface InternalUser extends PublicUser {
  phoneHash: string
  userId: string
  password?: string
  verificationCode?: string
  verificationCodeExpiredAt?: Date
  lastCodeSentAt?: Date
}

/**
 * 分页结果类型
 */
export interface UserPaginatedResult {
  list: PublicUser[]
  total: number
  pageNum: number
  pageSize: number
}

/**
 * 删除结果类型
 */
export interface UserDeleteResult {
  deleted: boolean
}
```

---

## 五、示例二：NFT 业务模块（带关联实体）

### 功能描述
复杂的业务模块，包含：NFT 类型管理、NFT 实例管理、关联查询。

### 5.1 模块结构

```
nft/
├── constants/                      # 常量层
│   ├── index.ts
│   └── nft.constants.ts
├── controllers/                    # 控制器层
│   ├── nft-instances.controller.ts
│   └── nft-types.controller.ts
├── dto/                           # DTO 层
│   ├── create-nft.dto.ts
│   ├── create-nft-instance.dto.ts
│   ├── index.ts
│   ├── nft-instance-response.dto.ts
│   ├── nft-response.dto.ts
│   ├── query-nft-instances.dto.ts
│   └── query-nft-types.dto.ts
├── entities/                      # 实体层
│   ├── nft.entity.ts
│   └── nft-instance.entity.ts
├── services/                      # 服务层
│   ├── nft-instances.service.ts
│   └── nft-types.service.ts
└── nft.module.ts                 # 模块定义
```

### 5.2 实体定义（关联关系）

```typescript
// src/modules/nft/entities/nft.entity.ts

import { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, UpdateDateColumn, OneToMany } from 'typeorm'
import { NftInstance } from './nft-instance.entity'

/**
 * NFT 类型实体
 */
@Entity('nft_types')
export class NftType {
  @PrimaryGeneratedColumn('uuid')
  id: string

  @Column({ length: 100 })
  name: string

  @Column({ type: 'text', nullable: true })
  description: string

  @Column({ length: 500, nullable: true })
  imageUrl: string

  @Column({ type: 'int', default: 0 })
  totalSupply: number

  @Column({ type: 'decimal', precision: 10, scale: 2, nullable: true })
  price: number

  @Column({ type: 'tinyint', default: 1 })
  status: number

  @OneToMany(() => NftInstance, (instance) => instance.nftType)
  instances: NftInstance[]

  @CreateDateColumn()
  createdAt: Date

  @UpdateDateColumn()
  updatedAt: Date
}
```

```typescript
// src/modules/nft/entities/nft-instance.entity.ts

import { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, UpdateDateColumn, ManyToOne, JoinColumn } from 'typeorm'
import { NftType } from './nft.entity'

/**
 * NFT 实例实体
 */
@Entity('nft_instances')
export class NftInstance {
  @PrimaryGeneratedColumn('uuid')
  id: string

  @Column({ length: 100, unique: true })
  tokenId: string

  @Column({ length: 100 })
  name: string

  @Column({ type: 'text', nullable: true })
  description: string

  @Column({ length: 500, nullable: true })
  imageUrl: string

  @Column({ length: 500, nullable: true })
  metadataUrl: string

  @Column({ length: 100, nullable: true })
  ownerAddress: string

  @Column({ type: 'tinyint', default: 0 })
  // 0: 未售出, 1: 已售出, 2: 转账中, 3: 已转账
  status: number

  @Column({ type: 'uuid', nullable: true })
  nftTypeId: string

  @ManyToOne(() => NftType, (nftType) => nftType.instances)
  @JoinColumn({ name: 'nftTypeId' })
  nftType: NftType

  @Column({ type: 'uuid', nullable: true })
  ownerId: string

  @Column({ type: 'datetime', nullable: true })
  mintedAt: Date

  @Column({ type: 'datetime', nullable: true })
  soldAt: Date

  @CreateDateColumn()
  createdAt: Date

  @UpdateDateColumn()
  updatedAt: Date
}
```

### 5.3 DTO 定义（带验证）

```typescript
// src/modules/nft/dto/create-nft.dto.ts

import { IsString, IsOptional, IsNumber, Min, Max, IsEnum } from 'class-validator'
import { NFT_CONSTANTS } from '@/modules/nft/constants'

/**
 * 创建 NFT 类型 DTO
 */
export class CreateNftDto {
  @IsString()
  name: string

  @IsString()
  @IsOptional()
  description?: string

  @IsString()
  @IsOptional()
  imageUrl?: string

  @IsNumber()
  @Min(0)
  @IsOptional()
  totalSupply?: number

  @IsNumber()
  @Min(0)
  @IsOptional()
  price?: number

  @IsEnum(NFT_CONSTANTS.STATUS)
  @IsOptional()
  status?: number
}
```

```typescript
// src/modules/nft/dto/query-nft-types.dto.ts

import { IsOptional, IsNumber, Min, Max, IsString, IsEnum } from 'class-validator'
import { Type } from 'class-transformer'
import { NFT_CONSTANTS } from '@/modules/nft/constants'

/**
 * 查询 NFT 类型列表 DTO
 */
export class QueryNftTypesDto {
  @IsNumber()
  @IsOptional()
  @Type(() => Number)
  @Min(1)
  page?: number = NFT_CONSTANTS.PAGINATION.DEFAULT_PAGE

  @IsNumber()
  @IsOptional()
  @Type(() => Number)
  @Min(1)
  @Max(100)
  limit?: number = NFT_CONSTANTS.PAGINATION.DEFAULT_LIMIT

  @IsString()
  @IsOptional()
  name?: string

  @IsEnum(NFT_CONSTANTS.STATUS)
  @IsOptional()
  status?: number
}
```

### 5.4 服务层（关联查询）

```typescript
// src/modules/nft/services/nft-types.service.ts

import { Injectable, NotFoundException, InternalServerErrorException } from '@nestjs/common'
import { InjectRepository } from '@nestjs/typeorm'
import { Repository } from 'typeorm'
import { NftType } from '@/modules/nft/entities/nft.entity'
import { CreateNftDto } from '@/modules/nft/dto'
import { QueryNftTypesDto } from '@/modules/nft/dto/query-nft-types.dto'
import { ApiResponse } from '@/common/interceptors/response.interceptor'
import { ResponseHelper } from '@/common/utils/response.helper'
import { NFT_CONSTANTS } from '@/modules/nft/constants'
import { NftTypePaginatedResult, NftTypeResponse } from '@/modules/nft/dto/nft-response.dto'

/**
 * NFT 类型服务
 */
@Injectable()
export class NftTypesService {
  constructor(
    @InjectRepository(NftType)
    private readonly nftTypeRepository: Repository<NftType>,
  ) {}

  /**
   * 创建 NFT 类型
   */
  async create(createNftDto: CreateNftDto): Promise<ApiResponse<NftTypeResponse>> {
    try {
      const nftType = this.nftTypeRepository.create(createNftDto)
      const result = await this.nftTypeRepository.save(nftType)
      return ResponseHelper.success(this._toResponse(result), 'NFT类型创建成功')
    } catch (error) {
      throw new InternalServerErrorException(
        'NFT类型创建失败，请稍后重试',
        { cause: error }
      )
    }
  }

  /**
   * 分页查询 NFT 类型列表
   */
  async findAll(query: QueryNftTypesDto): Promise<ApiResponse<NftTypePaginatedResult>> {
    try {
      const { page = 1, limit = 10, name, status } = query
      const safeLimit = Math.min(limit, NFT_CONSTANTS.PAGINATION.MAX_LIMIT)

      const queryBuilder = this.nftTypeRepository.createQueryBuilder('nft_type')

      // 动态添加查询条件
      if (name) {
        queryBuilder.andWhere('nft_type.name LIKE :name', { name: `%${name}%` })
      }
      if (status !== undefined) {
        queryBuilder.andWhere('nft_type.status = :status', { status })
      }

      // 分页查询
      const [list, total] = await queryBuilder
        .orderBy('nft_type.createdAt', 'DESC')
        .skip((page - 1) * safeLimit)
        .take(safeLimit)
        .getManyAndCount()

      return ResponseHelper.paginated(
        list.map(item => this._toResponse(item)),
        total,
        page,
        safeLimit,
        'NFT类型列表查询成功'
      )
    } catch (error) {
      throw new InternalServerErrorException(
        'NFT类型查询失败，请稍后重试',
        { cause: error }
      )
    }
  }

  /**
   * 根据 ID 查询 NFT 类型详情
   */
  async findOne(id: string): Promise<ApiResponse<NftTypeResponse>> {
    try {
      const nftType = await this.nftTypeRepository.findOne({
        where: { id }
      })

      if (!nftType) {
        throw new NotFoundException('NFT类型不存在')
      }

      return ResponseHelper.success(this._toResponse(nftType), 'NFT类型查询成功')
    } catch (error) {
      if (error instanceof NotFoundException) {
        throw error
      }
      throw new InternalServerErrorException(
        'NFT类型查询失败，请稍后重试',
        { cause: error }
      )
    }
  }

  /**
   * 转换为响应对象
   */
  private _toResponse(nftType: NftType): NftTypeResponse {
    return {
      id: nftType.id,
      name: nftType.name,
      description: nftType.description,
      imageUrl: nftType.imageUrl,
      totalSupply: nftType.totalSupply,
      price: nftType.price,
      status: nftType.status,
      createdAt: nftType.createdAt,
      updatedAt: nftType.updatedAt
    }
  }
}
```

### 5.5 控制器层

```typescript
// src/modules/nft/controllers/nft-types.controller.ts

import { 
  Controller, 
  Get, 
  Post, 
  Put, 
  Delete, 
  Body, 
  Param, 
  Query,
  ParseUUIDPipe,
  UseGuards,
} from '@nestjs/common'
import { NftTypesService } from '@/modules/nft/services/nft-types.service'
import { CreateNftDto } from '@/modules/nft/dto'
import { QueryNftTypesDto } from '@/modules/nft/dto/query-nft-types.dto'
import { JwtAuthGuard } from '@/modules/auth/guards/jwt.guard'

/**
 * NFT 类型控制器
 */
@Controller('api/nft/types')
@UseGuards(JwtAuthGuard)
export class NftTypesController {
  constructor(
    private readonly nftTypesService: NftTypesService,
  ) {}

  /**
   * 创建 NFT 类型
   */
  @Post()
  async create(@Body() createNftDto: CreateNftDto) {
    return await this.nftTypesService.create(createNftDto)
  }

  /**
   * 查询 NFT 类型列表（分页）
   */
  @Get()
  async findAll(@Query() query: QueryNftTypesDto) {
    return await this.nftTypesService.findAll(query)
  }

  /**
   * 查询 NFT 类型详情
   */
  @Get(':id')
  async findOne(@Param('id', ParseUUIDPipe) id: string) {
    return await this.nftTypesService.findOne(id)
  }

  /**
   * 更新 NFT 类型
   */
  @Put(':id')
  async update(
    @Param('id', ParseUUIDPipe) id: string,
    @Body() updateNftDto: CreateNftDto,
  ) {
    return await this.nftTypesService.update(id, updateNftDto)
  }

  /**
   * 删除 NFT 类型
   */
  @Delete(':id')
  async remove(@Param('id', ParseUUIDPipe) id: string) {
    return await this.nftTypesService.remove(id)
  }
}
```

### 5.6 模块定义

```typescript
// src/modules/nft/nft.module.ts

import { Module } from '@nestjs/common'
import { TypeOrmModule } from '@nestjs/typeorm'
import { NftType } from '@/modules/nft/entities/nft.entity'
import { NftInstance } from '@/modules/nft/entities/nft-instance.entity'
import { NftTypesController } from '@/modules/nft/controllers/nft-types.controller'
import { NftInstancesController } from '@/modules/nft/controllers/nft-instances.controller'
import { NftTypesService } from '@/modules/nft/services/nft-types.service'
import { NftInstancesService } from '@/modules/nft/services/nft-instances.service'

/**
 * NFT 模块
 */
@Module({
  imports: [
    TypeOrmModule.forFeature([NftType, NftInstance])
  ],
  controllers: [NftTypesController, NftInstancesController],
  providers: [NftTypesService, NftInstancesService],
  exports: [NftTypesService, NftInstancesService],
})
export class NftModule {}
```

---

## 六、六层结构解析

### 1. 基础层 (Foundation)
```typescript
// 导入 NestJS 核心模块
import { Controller, Get, Post, Body, Param, Injectable, NotFoundException } from '@nestjs/common'

// 导入 TypeORM
import { InjectRepository } from '@nestjs/typeorm'
import { Repository } from 'typeorm'

// 导入验证装饰器
import { IsString, IsNumber, IsOptional } from 'class-validator'

// 导入工具类
import { ResponseHelper } from '@/common/utils/response.helper'
```

### 2. 常量层 (Constants)
```typescript
// 统一管理业务常量
export const USER_CONSTANTS = {
  PAGINATION: {
    DEFAULT_PAGE: 1,
    DEFAULT_LIMIT: 10,
    MAX_LIMIT: 100
  },
  SELECT_FIELDS: {
    PUBLIC: ['id', 'username', 'email'] as const,
    INTERNAL: ['id', 'username', 'password'] as const
  }
}
```

### 3. DTO 层 (Data Transfer Object)
```typescript
// 数据传输对象 + 验证规则
export class CreateUserDto {
  @IsString()
  @MinLength(2)
  username: string

  @IsString()
  @MinLength(6)
  password: string

  @IsEmail()
  email: string
}
```

### 4. 实体层 (Entity)
```typescript
// 数据库实体定义
@Entity('users')
export class User {
  @PrimaryGeneratedColumn('uuid')
  id: string

  @Column({ length: 50 })
  username: string
}
```

### 5. 服务层 (Service)
```typescript
// 业务逻辑处理
@Injectable()
export class UserService {
  async getUserById(id: string) {
    const user = await this.userRepository.findOne({ where: { id } })
    if (!user) {
      throw new NotFoundException('用户不存在')
    }
    return ResponseHelper.success(user)
  }
}
```

### 6. 控制器层 (Controller)
```typescript
// HTTP 请求处理
@Controller('api/users')
export class UserController {
  @Get(':id')
  async getUserById(@Param('id') id: string) {
    return await this.userService.getUserById(id)
  }
}
```

---

## 七、核心规范要点

✅ **必须遵守的规范**
- 使用 TypeScript 进行开发
- 使用 `class-validator` 进行 DTO 验证
- 使用 `ResponseHelper` 返回统一格式响应
- 使用 UUID 作为主键（`PrimaryGeneratedColumn('uuid')`）
- 使用软删除（`isActive` 字段）
- 所有方法添加中文注释
- 控制器方法添加 JSDoc 注释

❌ **禁止的做法**
- 不使用裸 SQL（使用 TypeORM QueryBuilder）
- 不直接返回数据库实体（转换为 DTO/Response 对象）
- 不在服务层处理 HTTP 响应（使用拦截器统一处理）
- 不使用 `any` 类型
- 不硬编码业务常量

---

## 八、开发检查清单

### 开发前检查
- [ ] 已阅读技术栈规范文档
- [ ] 已确认模块功能需求
- [ ] 已确认数据库实体字段
- [ ] 已确认接口文档

### 代码编写检查
- [ ] TypeScript 类型定义完整
- [ ] DTO 添加验证装饰器
- [ ] Service 添加中文注释
- [ ] Controller 添加 JSDoc 注释
- [ ] 使用 `ResponseHelper` 返回响应
- [ ] 正确处理异常（NotFoundException、InternalServerErrorException）
- [ ] 使用 UUID 主键
- [ ] 使用软删除
- [ ] 模块正确导出依赖

### 模块注册检查
- [ ] 在 `BusinessModule` 中导入新模块
- [ ] 在 `app.module.ts` 中导入 `BusinessModule`

---

## 九、常见问题 FAQ

### Q1: 为什么要使用六层结构？
**A:** 六层结构让代码职责清晰、易于维护：
- 基础层：明确依赖关系
- 常量层：统一管理配置
- DTO 层：数据验证
- 实体层：数据库映射
- 服务层：业务逻辑
- 控制器层：请求处理

### Q2: 什么时候使用 `ParseUUIDPipe`？
**A:** 当 URL 参数是 UUID 类型时使用，例如：
```typescript
@Get(':id')
async getUser(@Param('id', ParseUUIDPipe) id: string) {}
```

### Q3: 为什么要使用软删除？
**A:** 软删除保留数据，只设置 `isActive = 0`，便于数据恢复和审计。

### Q4: 为什么不直接返回实体？
**A:** 实体可能包含敏感字段，应转换为 DTO 或 Response 对象后再返回。

### Q5: 如何处理分页？
**A:** 使用 `QueryBuilder` 或 `findAndCount`，结合 `skip` 和 `take`：
```typescript
const [list, total] = await repository.findAndCount({
  skip: (page - 1) * limit,
  take: limit
})
```

### Q6: 如何统一返回格式？
**A:** 使用 `ResponseHelper` 或全局响应拦截器：
```typescript
return ResponseHelper.success(data, '操作成功')
return ResponseHelper.paginated(list, total, page, limit)
```

---

## 十、文档版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|---------|--------|
| v1.0 | 2026-03-09 | 初始版本，定义六层结构和 NestJS 开发规范 | AI Assistant |

---

**最后更新时间：** 2026年3月9日  
**文档维护：** 开发团队  
**联系方式：** 请通过项目 Issue 反馈问题和建议
